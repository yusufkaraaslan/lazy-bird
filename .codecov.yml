codecov:
  require_ci_to_pass: yes

coverage:
  precision: 2
  round: down
  range: "10...100"

  status:
    project:
      default:
        target: 10%
        threshold: 5%
        if_ci_failed: error

    patch:
      default:
        target: 10%
        threshold: 5%
        informational: true  # Don't fail builds on patch coverage

comment:
  layout: "header, diff, flags, files, footer"
  behavior: default
  require_changes: no
  require_base: no
  require_head: yes

ignore:
  - "tests/"
  - "**/__pycache__"
  - "**/*.pyc"
  - "setup.py"
